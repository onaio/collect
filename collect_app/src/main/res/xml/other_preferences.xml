<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android" >

    <PreferenceCategory
        android:key="@string/other_preferences"
        android:title="@string/other_preferences" >
        <EditTextPreference
            android:id="@+id/api_url"
            android:defaultValue="@string/default_api_url"
            android:dialogTitle="@string/change_api_url"
            android:inputType="textNoSuggestions"
            android:key="api_url"
            android:layout="?android:attr/preferenceLayoutChild"
            android:title="@string/api_url" />
        <EditTextPreference
            android:id="@+id/formlist_url"
            android:defaultValue="@string/default_odk_formlist"
            android:dialogTitle="@string/change_formlist_url"
            android:inputType="textNoSuggestions"
            android:key="formlist_url"
            android:layout="?android:attr/preferenceLayoutChild"
            android:title="@string/formlist_url" />
        <EditTextPreference
            android:id="@+id/submission_url"
            android:defaultValue="@string/default_odk_submission"
            android:dialogTitle="@string/change_submission_url"
            android:inputType="textNoSuggestions"
            android:key="submission_url"
            android:layout="?android:attr/preferenceLayoutChild"
            android:title="@string/submission_url" />
        <EditTextPreference
            android:id="@+id/projects_url"
            android:defaultValue="@string/default_ona_projects"
            android:dialogTitle="@string/change_projects_url"
            android:inputType="textNoSuggestions"
            android:key="projects_url"
            android:layout="?android:attr/preferenceLayoutChild"
            android:title="@string/projects_url" />
    </PreferenceCategory>
   
</PreferenceScreen>